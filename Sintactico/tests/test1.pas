program test;
const constante = 1;
var c : char;
x,y,z : integer;
c : char;
integer : char;
b : charc;
procedure holi;
begin
  write("holi")
end;
procedure chau;
begin
  write("chau")
end;
begin
  x := -1 * "abc" + (3 - 5);
  y := 5 div 3 div 5 - 3 * (3*5 - 2) div 2;
  z := 3;
  c := "holiwi";
  c := 1;
  constante := 20;
  read(x,y,z);
  write(x,y,x - y);
  read(y);
  if -x <> y * 500 then
    write(x)
  else
    write(y);
  read(z);
  if not "holi" = (y or 7 = 5) then
  begin
    write("hola");
    write("chao")
  end
  else
  begin
    read(z);
    y := x + "chai"
  end;
  read(x)
end.
