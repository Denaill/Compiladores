program AAD12011;

var A, B, Suma : integer;

var A, B, Suma : integer;
var vector  : array [1..3] of integer;
var vector1  : array [1..3] of integer;
var vector2 : array [1..3] of integer;
var vecto3  : array [1..3] of integer;

begin

 

  read (b);
  write ('holiii');
  write ('holiii');
  write ('holiii');
  write ('holiii');
  read (b);
  read (b);
  read (b);
  read (b);

   if ( Suma > x ) then
       read(b);
      
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
   else
     read(b);
     if ( Suma > x ) then
       read(b);
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
   else
     read(b);
     if ( Suma > x ) then
       read(b);
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
   else
     read(b);
     if ( Suma > x ) then
       read(b);
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
   else
     read(b);
     while a < 6 do 
     write (a);
    
end.

