program AAD12011;



begin


  read (b);
  write ('holiii');
  write ('holiii');
  write ('holiii');
  write ('holiii');
  read (b);
  read (b);
  read (b);
  read (b);

   if ( Suma > x ) then
       read(b);
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
   else
     read(b);
     if ( Suma > x ) then
       read(b);
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
   else
     read(b);
     if ( Suma > x ) then
       read(b);
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
   else
     read(b);
     if ( Suma > x ) then
       read(b);
   else
     read(b);

    if ( Suma > x ) then
       read(b);
   else
     read(b);
    if ( Suma > x ) then
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
       read(b);
   else
     read(b);
    
end.

